---
kind: fixed
---

While renaming a session or using the session launcher over an open session, a terminal tab that closed or exited
could move keyboard focus to the agent's terminal, so the rest of what you typed went to the agent. Focus now stays in
the dialog, as it already did for "restart with".
