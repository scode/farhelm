---
kind: changed
---

The session's action buttons (restart, replace, clone, replace with) are now lowercase, matching the rest of Farhelm's
controls. The same applies to the restart and replace buttons on the card shown after a host restart paused a session.
