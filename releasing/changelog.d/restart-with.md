---
kind: added
---

You can use `restart with` in a session's header to change its model, effort, permissions, or workspace trust and resume the same conversation. The session's harness, host, and folder stay fixed.
