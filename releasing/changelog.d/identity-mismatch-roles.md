---
kind: fixed
---

When checking the local host or planning an update found a supervisor reporting a different identity from the one on
record, the error named the two identities the wrong way round, calling the newly reported one the identity on record.
It now matches the wording of the same error when adding an ssh host.
