---
kind: changed
---

The session launcher's "reset choices" control now looks like a button, in the same secondary style as the other
secondary actions, instead of plain grey text that only revealed itself on hover.
