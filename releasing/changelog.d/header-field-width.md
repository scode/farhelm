---
kind: fixed
---

The folder and command shown at the top of an open session now use the space the header has before cutting off with
an ellipsis. Previously they were cut short after a handful of characters even in a wide window, leaving a large empty
gap before the action buttons.
