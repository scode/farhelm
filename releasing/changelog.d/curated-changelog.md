---
kind: added
---

Every stable release now carries curated release notes: the matching section of `CHANGELOG.md` appears on the GitHub release page above the download tables. Release candidates and dev builds carry none.
