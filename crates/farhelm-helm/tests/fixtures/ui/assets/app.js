// Step 1 embedded-UI test fixture: farhelm-helm/tests/fixtures/ui/assets.
// Distinctive contents so a test can assert on the response body and on the
// `mime_guess`-derived content type (`.js` -> text/javascript) together.
console.log("farhelm-embedded-ui-fixture-app-js");
