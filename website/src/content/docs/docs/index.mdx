---
title: Farhelm documentation
description: Documentation for running Farhelm and the coding agents it manages.
---

import ThemedImage from '../../../components/ThemedImage.astro';
import headerDark from '../../../assets/intro/header-dark.svg';
import headerLight from '../../../assets/intro/header-light.svg';
import howItWorksDark from '../../../assets/intro/how-it-works-dark.svg';
import howItWorksLight from '../../../assets/intro/how-it-works-light.svg';
import pillarsDark from '../../../assets/intro/pillars-dark.svg';
import pillarsLight from '../../../assets/intro/pillars-light.svg';

{/* This introduction is the README's, and the two share their drawings: the SVGs under src/assets/intro/ are
    rendered by scripts/render-svgs.mjs and the README links to the same files. Change a drawing there, and change
    the prose here and in the README together. */}

<ThemedImage
  class="farhelm-intro-header"
  light={headerLight}
  dark={headerDark}
  alt="farhelm: take the helm of your agents, far away and local"
/>

One screen for every coding agent you run, on every machine you own. Real terminals over your own SSH, still running on
the remote box when you close the lid.

{/* The screenshot is the README's hero capture, hosted on the readme-assets branch rather than committed on main (see
    docs/readme-hero/SPEC.md). scripts/publish-readme-hero.sh rewrites the URL between the two markers below along with
    the README's; do not edit it by hand. */}

{/* readme-hero-url */}

![Farhelm supervising seven agent sessions across three hosts, with one session's Claude Code terminal
open](https://raw.githubusercontent.com/scode/farhelm/255d75fc338366e3069f68410a5ef474c66187a1/readme-hero.png)

{/* /readme-hero-url */}

<p class="farhelm-intro-caption">Seven sessions across three hosts. The status dot on each row is the supervisor's own read of the live terminal.</p>

<ThemedImage
  light={pillarsLight}
  dark={pillarsDark}
  alt="What Farhelm gives you: durable sessions, every host on one screen, the real terminal, SSH as the only network, status at a glance, any agent"
/>

## How it works

<ThemedImage
  light={howItWorksLight}
  dark={howItWorksDark}
  alt="How Farhelm works: the helm on your laptop lists every session from every host and reaches a supervisor on each remote box over ssh; the selected session's real terminal fills the pane"
/>

One **helm** is the control plane: it holds the host list, serves the UI, and connects straight to a **supervisor** on
each host over SSH. The supervisor owns the agents' real terminals and their state. Nothing listens on a network port,
nothing goes through a relay, and every box in the diagram can restart without losing a session.

## About these pages

These pages cover the individual agent harnesses Farhelm can launch and the mechanisms behind launching and resuming
them. The documentation is in the middle of moving here from the repository's `docs/` directory; pages not yet on this
site are still readable on [GitHub](https://github.com/scode/farhelm/tree/main/docs).
